from animal import Animal
from enclosure  import Enclosure
from visitors import Visitors
from player import Player
class Zoo:
    def __init__(self, name):
        self.name = name
        self.visitors = 0
        self.enclosures = {}
        self.listAnimais = {}
        self.animal

    # def criarAnimal(self,name,species,happiness_level): ):
    #     if tipo == "leao":
    #         a = Animal(self, "zimba","leao",50)
    #         self.listAnimais["zimba"] = a



    def receive_visitor_for_a_unique_enclousure(self, enclousre, visitor, animal):
        # visitor = Visitors(self, visitor)
        visitors += 1
        if Enclosure.good_maintain == True and Animal.verifica_felicidade == "O animal está feliz":
            Player.win_money(100)
            return "O animal está feliz"

            
